import { Box } from "@chakra-ui/react";
import styled from "styled-components";

export const EventCard = styled(Box)`
  border-width: 1px;
  border-radius: 10px;
  margin: 4;
  cursor: pointer;
`;

export const ComponentsEventCard = styled.div`
  padding: calc(20px - 5px);
  box-sizing: border-box;
`;
